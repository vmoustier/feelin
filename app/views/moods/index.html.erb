<p id="notice"><%= notice %></p>

<h1 align="center">How do you feel?</h1>

    <% @moods.each do |mood| %>
      <%= link_to mood.title, add_user_moods_path(user_id: current_user, mood_id: mood),
                  class: 'btn btn-lg btn-danger', method: :post %>
    <% end %>

<br>