<%= form_for(@user_mood) do |f| %>
  <% if @user_mood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_mood.errors.count, "error") %> prohibited this user_mood from being saved:</h2>

      <ul>
      <% @user_mood.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :mood_id %><br>
    <%= f.number_field :mood_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
